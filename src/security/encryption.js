// Boundary only. Replace with selected audited E2EE implementation.
export const E2EE_PROTOCOL='NOT_CONFIGURED';
export function encryptMessage(){ throw new Error('E2EE implementation not configured'); }
export function decryptMessage(){ throw new Error('E2EE implementation not configured'); }
